{
  "name": "yields",
  "version": "0.1.0",
  "private": true,
  "description": "AU investment portfolio tracker + planner",
  "scripts": {
    "dev": "pnpm -r --parallel --filter @yields/api --filter @yields/web dev",
    "dev:api": "pnpm --filter @yields/api dev",
    "dev:web": "pnpm --filter @yields/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.5.0"
}
